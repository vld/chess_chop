- content_for :css do
  = stylesheet_link_tag 'the_sortable_tree_min', :media => :screen

%p= link_to raw('&larr; back'), root_path

= sortable_tree @categories, :title => :name, :path => 'the_sortable_tree_min'
