- content_for :css do
  = stylesheet_link_tag 'the_sortable_tree', :media => :screen
- content_for :js do
  = javascript_include_tag 'jquery.ui.nestedSortable'

%p= link_to raw('&larr; ' + t(:nav_back)), '/'

= sortable_tree @categories, :title => :name, :new_url => new_category_path, :max_levels => 4
