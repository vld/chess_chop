%ul.items
  - for item in items
    %li
      = link_to image_tag(item.attachments[item.main_image].image.thumb.url), item
      %span.title= link_to truncate(item.title, :length => 20), item
      = link_to item.category.name, category_path(item.category)
      %span.price= item.price
